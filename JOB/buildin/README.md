# 作业内建插件说明/x

cdn刷新插件需要安装如下依赖
```

# C3TODO 230303 作业平台的插件应该使用pkg中的python
# pkg中有C3已经打包好的python，应该使用/data/Software/mydan/python3/bin/python3
# 如果有缺少的模块就可以进行统一管理

cd /tmp
curl https://bootstrap.pypa.io/pip/2.7/get-pip.py -o get-pip.py 
python get-pip.py

pip install pyyaml
```

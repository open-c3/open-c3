download:
  - url: "cmd:c3mc-cloud-qcloud-redis ak sk ap-beijing | c3mc-base-json2tab InstanceId 实例ID"
    datatype: tsv
    type: database
    subtype: qcloud-redis
    init: ["c3mc-cloud-qcloud.sh"]
    treeinfo:
      treename: "服务树"
      treeinfo:
        - ["实例ID", "instance"]
    check:
      col: ["实例ID"]
      min: 0
    util:
      defaulttree: defaulttree.pool
    colmap:
      treename: "服务树"
      uuid: "实例ID" #没有这个可能导致单个资源详情打开无数据
    outline:
      uuid:
        - "实例ID"
      baseinfo:
        - "ZoneId"
      system:
        - "ConnectionString"
      contact:
        - "服务树"

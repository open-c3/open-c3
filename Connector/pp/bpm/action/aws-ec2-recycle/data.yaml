---
name: 回收aws ec2
option:
  - name: recycle_type
    describe: 回收类型
    command: c3mc-server-recycle-type | c3mc-bpm-display-field-values id,desc
    type: selectx

  - name: instance_ids
    describe: 实例id列表 (多个用英文逗号分隔)
    type: text

template_argv:
  recycle_type: ${recycle_type}
  instance_ids: ${instance_ids}

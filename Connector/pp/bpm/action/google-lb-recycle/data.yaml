---
name: 回收谷歌云lb
option:
  - name: instance_ids
    describe: 实例id列表 (多个用英文逗号分隔)
    type: text
    information: "实例id是openc3中实例详情页面的 '实例id' 字段的值"

template_argv:
  # bpm单号
  bpm_uuid: ${_bpmuuid_}
  # lb实例列表
  instance_ids: ${instance_ids}
